on:
  workflow_dispatch:

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - run: |
          cat /etc/resolv.conf
          resolvectl status
          systemctl status docker
          systemctl restart docker
          systemctl status docker
